#include<bits/stdc++.h>
using namespace std;
int main(){
    cout<<"Radhe_Radhe"<<endl;
    string s;
    int len = s.length();
    getline(cin, s);
    reverse(s.begin(), s.begin()+4);
    cout<<s<<endl;
return 0;
}