#include<bits/stdc++.h>
using namespace std;
int main(){
    string str;
    cout<<"Radhe radhe"<<endl; 
    cout<<"Enter the string"<<endl;
    getline(cin, str);
    /*int maxcount=1;
    //to find the frequency of the chsacter appearing maximum number of times
    for(int i =0; i<str.length(); i++){
        int count =1;
        for(int j = i+1; j< str.length(); j++){
            if(str[i] == str[j]) count++;
        }
        if(maxcount<count) maxcount = count;
    }
    // cout<<maxcount;
    for(int i =0; i<str.length(); i++){
        int count =1;
        for(int j = i+1; j< str.length(); j++){
            if(str[i] == str[j]) count++;
        }
        if(maxcount == count)
        cout<<str[i]<<" "<<count<<endl;
    }      
    */
   // METHOD_NO.2
   
        return 0;
}